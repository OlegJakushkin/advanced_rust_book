# Google Analytics 4 Measurement ID for the deployed (GitHub Pages) build.
# GA4 Measurement IDs are not secrets — gtag.js exposes them in the client bundle
# on every page — so this is safe to commit. Loaded automatically by `next build`.
NEXT_PUBLIC_GA_ID=G-RTMW0NF138
